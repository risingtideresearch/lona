{
  "name": "lona-geom",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "main": "dist/lona-geom.js",
  "types": "dist/main.d.ts",
  "exports": {
    ".": {
      "types": "./dist/main.d.ts",
      "import": "./dist/lona-geom.js"
    }
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "clean": "node ../../scripts/clean.mjs dist",
    "test": "vitest run",
    "test-watch": "vitest",
    "dev": "vite",
    "watch": "npm run clean && vite build --watch",
    "build": "npm run clean && tsc --noEmit && vite build",
    "preview": "vite preview",
    "lint": "eslint --max-warnings 0 .",
    "typecheck": "tsc --noEmit",
    "format": "prettier --ignore-path ../../.prettierignore --write .",
    "formatcheck": "prettier --ignore-path ../../.prettierignore --check .",
    "check": "npm run typecheck && npm run lint && npm run test && npm run formatcheck"
  },
  "peerDependencies": {
    "lona": "*"
  }
}
